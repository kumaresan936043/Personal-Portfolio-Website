# Personal-Portfolio-Website
Yes. A simple way to build the portfolio entirely in Python is Flask + SQLite. SQLite is a good starting database because it requires no separate database server; you can later switch to MySQL/PostgreSQL.  Here is a complete starter project:
